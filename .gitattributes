# Prevent git from converting line endings in any text file.
# Files are stored with whatever bytes are on disk (LF).
# Use -text so git never normalises EOLs, preserving exact bytes.
*           -text
